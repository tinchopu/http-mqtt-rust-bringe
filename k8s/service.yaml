apiVersion: v1
kind: Service
metadata:
  name: garage-mqtt-bridge
  namespace: default  # Change to your namespace
  labels:
    app: garage-mqtt-bridge
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: garage-mqtt-bridge
